using NUnit.Framework;
using UnityEngine;
using System;
//using Tetris.Model;
//using Tetris.Exceptions;

public class GridTest : MonoBehaviour {

	//[Test]
 //   public void ConstructorTest()
 //   {
 //       var grid = ScriptableObject.CreateInstance(typeof(TetrisGridModel)) as TetrisGridModel;
 //       Assert.IsNotNull(grid);

 //       Assert.Catch<NullReferenceException>(() => { var cell = grid.Cells[0, 0]; });

 //       Assert.Catch<GridGenerationException>(() => grid.InitGrid(new Vector2Int(0, 0)));
 //       Assert.Catch<GridGenerationException>(() => grid.InitGrid(new Vector2Int(1, 0)));
 //       grid.InitGrid(new Vector2Int(1, 1));
 //       Assert.IsNull(grid.Cells[0,0]);
 //   }
}
